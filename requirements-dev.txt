# Todas las dependencias de producción
-r requirements.txt

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-asyncio>=0.21.0

# Desarrollo y debugging
ipython>=8.17.0

# Lint en CI (solo pyflakes, ver ruff.toml). black/mypy quedan afuera hasta
# que haga falta de verdad -- ver specs/change_logs.md.
ruff>=0.1.8
