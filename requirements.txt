# ==========================================
# CORE & API FRAMEWORK
# ==========================================
fastapi>=0.115.0
uvicorn[standard]>=0.31.0
pydantic>=2.13.0
pydantic-settings>=2.14.0

# ==========================================
# BASE DE DATOS & VECTORES (PostgreSQL + pgvector)
# ==========================================
sqlmodel>=0.0.16
sqlalchemy>=2.0.41
psycopg[binary]>=3.1.18
pgvector>=0.2.5
alembic>=1.13.1

# ==========================================
# INGESTA & EXTRACCIÓN DE NOTICIAS
# ==========================================
feedparser>=6.0.11
beautifulsoup4>=4.12.3
httpx>=0.27.0
tenacity>=8.2.3

# Segunda vía de ingesta (medios sin content:encoded, ver specs/roadmap.md,
# backlog priorizado punto 1): la comparación real ya se hizo y trafilatura fue
# la elegida frente a newspaper4k — ver specs/change_logs.md.
#
# **Con techo de versión mayor, y el motivo está medido.** `extract()` trae
# `include_comments=True` por default, así que durante toda la etapa 2 los
# comentarios de lectores podían entrar al cuerpo sin que nadie lo decidiera.
# Los parámetros que nos importan ahora van explícitos en `extraccion.py`, pero
# eso solo cubre los que ya conocemos: un major nuevo puede cambiar otro
# default y volver a decidir por nosotros qué texto guardamos. Subir de 3.x es
# una decisión que se toma leyendo el changelog, no un `pip install`.
trafilatura>=2.0.0,<3.0.0

# ==========================================
# VECTORIZACIÓN, NLP & CLUSTERING
# ==========================================
sentence-transformers>=2.6.0
# spaCy se usa para NER en el preproceso de la síntesis (Fase 4). El modelo en
# español NO viene con la librería y hay que bajarlo aparte, después de instalar
# estos requirements:
#     python -m spacy download es_core_news_md
spacy>=3.8.0
# scikit-learn se usa para el TF-IDF del preproceso (Fase 4).
scikit-learn>=1.5.0
numpy>=1.26.4

# ==========================================
# INTELIGENCIA ARTIFICIAL & MODELOS DE LENGUAJE
# ==========================================
google-genai>=0.3.0

# ==========================================
# AUTOMATIZACIÓN & UTILIDADES
# ==========================================
apscheduler>=3.10.4
python-dotenv>=1.0.1